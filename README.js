# ARCA NodeJS for WEB

In order to start the back-server, execute the following:
```
npm start
```

In order to start the front-server (example), execute the following:
```
http-server
```
Then navigate to http://localhost:8080/client.html
